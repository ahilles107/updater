language: php

php:
  - '5.3.9'
  - '5.3'
  - '5.4'
  - '5.5'
  - 'nightly'

matrix:
  allow_failures:
    php: 'nightly'
before_script:
  - composer self-update
  - composer install --dev --prefer-source

script: bin/phpspec run
